import { ReusableGridHeaderTemplateDirective } from './reusable-grid-header-template.directive';

describe('ReusableGridHeaderTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ReusableGridHeaderTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
