import { ReusableGridCellTemplateDirective } from './reusable-grid-cell-template.directive';

describe('ReusableGridCellTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ReusableGridCellTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
