import { ReusableGridNodataDirective } from './reusable-grid-nodata.directive';

describe('ReusableGridNodataDirective', () => {
  it('should create an instance', () => {
    const directive = new ReusableGridNodataDirective();
    expect(directive).toBeTruthy();
  });
});
